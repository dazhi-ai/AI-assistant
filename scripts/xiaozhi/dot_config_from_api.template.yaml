# Minimal data/.config.yaml: pull modules + agent system_prompt from manager-api (do NOT mix with local LLM/ASR/TTS blocks).
# Replace MANAGER_SECRET_PLACEHOLDER with 智控台 sys_params server.secret; set vision_explain to your public IP:8003.
server:
  ip: 0.0.0.0
  port: 8000
  http_port: 8003
  vision_explain: http://124.223.174.173:8003/mcp/vision/explain

manager-api:
  url: http://xiaozhi-esp32-server-web:8002/xiaozhi
  secret: MANAGER_SECRET_PLACEHOLDER

prompt_template: agent-base-prompt.txt
